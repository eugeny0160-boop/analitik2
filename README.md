# 🤖 Финансист-Аналитик — Генератор аналитики из Supabase

## Что делает бот?

- Читает статьи из таблицы `published_articles` в Supabase за последние 24 часа.
- Классифицирует их по категориям: Россия, СВО, Криптоволюта, Тенденции в Мире, Пандемия.
- Генерирует **аналитическую записку** по строгой структуре (Исполнительное резюме → ТОП-5 → Тематический анализ → Влияние на Россию → Китай/Евразия → Мир → Прогнозы).
- Отправляет отчёт в публичный канал.
- Работает как Web Service на Render с Gunicorn.

---

## 🚀 Как развернуть на Render

### 1. Подготовьте репозиторий

1.  Создайте **новый репозиторий на GitHub** (например, `analitik2`).
2.  Скопируйте **все файлы из этого архива** (`main.py`, `requirements.txt`, `.gitignore`, `README.md`) в **корень** репозитория.
3.  **НЕ копируйте `.env.example`** в репозиторий.
4.  Сделайте `git add .`, `git commit -m "Initial commit"`, `git push`.

### 2. Создайте таблицу в Supabase

В SQL Editor выполните:

```sql
CREATE TABLE published_articles (
    id BIGSERIAL PRIMARY KEY,
    url TEXT UNIQUE NOT NULL,
    title TEXT,
    content TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
